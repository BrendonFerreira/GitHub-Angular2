/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { MegaContentAPIService } from './mega-content-api.service';

describe('Service: MegaContentAPI', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [MegaContentAPIService]
    });
  });

  it('should ...', inject([MegaContentAPIService], (service: MegaContentAPIService) => {
    expect(service).toBeTruthy();
  }));

  it('should return ', (done)=>{
    done()
  })

});
